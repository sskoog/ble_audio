---
trigger: always_on
---

1. **Pre-Flash Change Summary**:
   - Always print a concise summary of recent source changes to the user before compiling and flashing firmware.

2. **Formatting Constraint**:
   - Format all chat and response text without LaTeX-style markdown (do NOT use `$` or `$$` notation for variables, equations, or math symbols). Use plain text or code backticks instead.

