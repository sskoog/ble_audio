#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @file wifi_credentials.h
 * @brief Wi-Fi Credentials & Over-The-Air (OTA) Firmware Server Configuration.
 * 
 * Copy this template file to `wifi_credentials.h` and configure with your network credentials.
 * IMPORTANT: `wifi_credentials.h` is ignored by git to prevent leaking network credentials.
 */

#define CONFIG_WIFI_SSID             "YOUR_WIFI_SSID"
#define CONFIG_WIFI_PASSWORD         "YOUR_WIFI_PASSWORD"

/* Over-The-Air (OTA) Firmware Binary Download URL */
#define CONFIG_OTA_FIRMWARE_URL      "http://192.168.1.100:8070/esp32c6_firmware.bin"

/* Maximum Wi-Fi Connection Retries */
#define CONFIG_WIFI_MAXIMUM_RETRY    5

#ifdef __cplusplus
}
#endif

#endif /* WIFI_CREDENTIALS_H */
